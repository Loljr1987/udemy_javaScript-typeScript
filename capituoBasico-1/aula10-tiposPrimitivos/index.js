// String, number, undefined, null, boolean

const nome = 'Lucivaldo';
const nome1 = "Lucivaldo1";
const nome2= `Lucivaldo2`; // No caso das `crases`, podemos utilizar template strings dentro delas.
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAlunoo; // undefined -> não aponta local algum na memória
const sobrenomeAluno = null; // Nulo -> não aponta local algum na memória (quando queremos desvalidar uma variavel)
const aprovado = true; // boolean = valores true ou false(valor lógico).
// typeof = para verificar o tipo de variável.

