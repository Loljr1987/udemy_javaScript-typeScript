// ECMAScript = É uma padrozinação da linguagem JavaScript pela empresa de mesmo nome.
var nome = 'Luci';
var nome = 'Junior';

// Pode ser declarada sem tipar ela (var, let ou const), porém será declarada de
// maneira global e pode gerar confito no futuro.
// Então não é aconselhavél fazer isso.
nome = 'Luci'; 

// Lembrando variável com 'var' podemos redeclarar ela posteriormente , 
console.log(nome);

