const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
// console.log(resultadoDuplicado);

console.log(typeof primeiroNumero); // a função 'typeof' identifica o tipo da variável.

// podemos utilizar varias 'const' só não poderemos redeclarar a mesma como podemos fazere com 'let'.